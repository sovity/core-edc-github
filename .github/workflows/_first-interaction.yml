name: First Interaction

on:
  issues:
    types: [ opened ]
  pull_request_target:
    types: [ opened ]

jobs:
  trigger-workflow:
    uses: .github/workflows/first-interaction.yml
    secrets:
      envGH: ${{ secrets.GITHUB_TOKEN }}