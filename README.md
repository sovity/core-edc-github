
# .github

Versions the GH actions are the original always used `@main` which is now outdated to build an old fork version.

The branch `fork/0.7.2.x` tracks the version that is in use with the `core-edc` fork `sovity/0.7.2`
